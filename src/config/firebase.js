import messaging from '@react-native-firebase/messaging';

// Firebase configuration will be automatically loaded from:
// - android/app/google-services.json (Android)
// - ios/ksp/GoogleService-Info.plist (iOS)

// No need to manually configure Firebase - it's auto-configured
// Just export messaging for receiving notifications
export { messaging };
